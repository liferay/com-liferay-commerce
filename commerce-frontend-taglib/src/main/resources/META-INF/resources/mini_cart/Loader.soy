{namespace Loader}

/**
 * This renders the component's whole content.
 */
{template .render}
	{@param? direction: string}
	{@param? inverted: bool}

	{let $loaderClasses kind="text"}
		minium-loader__dots-wrapper
		{if isNonnull($direction)}
			{sp}minium-loader__dots-wrapper--{$direction}
		{/if}
		{if $inverted}
			{sp}minium-loader__dots-wrapper--inverted
		{/if}
	{/let}

	<span class="minium-loader">
		<div class="{$loaderClasses}">
			<div class="minium-loader__dot"></div>
			<div class="minium-loader__dot"></div>
			<div class="minium-loader__dot"></div>
		</div>
	</span>
{/template}